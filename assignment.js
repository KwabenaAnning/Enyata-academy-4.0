let length = 30;
let width = 1

let area= length*width;

width+=2;

let perimeter = ((2*length)+(2*width));

console.log(`area=${length*width}`)
console.log(`perimeter =${(2*length)+(2*width)}`);

let areaSquare= Math.sqrt(area)
areaSquare=(`${Math.sqrt(area)}`);
console.log(`areaSquare= ${areaSquare}`)
let conversion=(area.toString().length)

console.log(`conversion=${conversion}`);



